model = "gpt-5.3-codex"
model_reasoning_effort = "high"

[features]
multi_agent = true
# codex_hooks is currently under development in Codex CLI.
# Keep disabled for now and revisit when it becomes stable.
codex_hooks = false
