# ------------------------------------------------------------
# PATH management
# ------------------------------------------------------------
# Centralized location for PATH modifications.
# Add new system paths here instead of editing .zshrc.
# ------------------------------------------------------------

# Ensure manually linked binaries (e.g., VS Code CLI) are accessible
export PATH="/usr/local/bin:$PATH"

# Uncomment if using Apple Silicon Homebrew
# export PATH="/opt/homebrew/bin:$PATH"